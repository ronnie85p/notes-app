<x-layout>
    <div class="card card-body">
        <p class="lead text-center">
            Для того чтобы создать заметку 
            <a class="" href="{{ route('auth.signin') }}">войдите</a> или 
            <a class="" href="{{ route('auth.signup') }}">зарегистрируйтесь</a>.
        </p>
    </div>
</x-layout>